

AI 智能体从只会说话到能干实事，再到能组队写作。  

| 概念                    | 回答的问题               | 层级        | 通俗类比          |
| --------------------- | ------------------- | --------- | ------------- |
| **Function Calling**​ | 这次调用哪个函数、参数是什么      | 执行层（原子机制） | 模型"开口说"要调工具   |
| **MCP**​              | 这个 Agent 能连上什么工具/数据 | 连接层（纵向）   | AI 的 USB-C 接口 |
| **Skill**​            | 这个组织里"这事该怎么办"       | 知识打包层     | 写给模型看的操作规程    |
| **A2A**​              | 这个任务该交给哪个 Agent     | 协作层（横向）   | 智能体之间的"微信群"   |


# Function Calling : 让模型开口要调工具

### 解决的问题

早期的大语言模型（LLM）只是个"嘴强王者"——它知道很多知识，但**不能执行任何操作**。你问它"今天北京天气怎么样"，它只能凭训练记忆编一个答案，而不是真的去查。

Function Calling（函数调用）机制的诞生，就是给模型"一双 hands"：当模型判断需要外部数据时，它就输出一段**结构化的 JSON 指令**，比如：
```json
{"tool": "get_weather", "city": "北京"}
```

你的代码执行这个指令 → 调用真实的天气 API → 拿到结果 → 把结果塞回给模型 → 模型再组织成自然语言回答用户。


函数调用(Function Call)是大语言模型的一种能力：模型不仅返回文本，还能按照约定的格式返回需要调用的外部函数/工具的名称和参数信息。

### 本质
Function Calling 是**各模型厂商的私有实现**——OpenAI、Anthropic、智谱等都有自己的函数调用格式。它解决的是"模型怎么表达调用意图"这个最原子的机制问题。

### 存在的问题

- **接口碎片化**：每个模型的函数调用格式不同，每个工具的 API 也不同。M 个模型 × N 个工具 = M×N 次定制开发。
- **接入成本高**：开发者需要为每个工具单独写适配代码，效率低下且难以维护。
- **发现机制缺失**：模型怎么知道"当前有哪些工具可用"？早期只能靠人工在 prompt 里硬写。

这就引出了 MCP。

# MCP ： 把工具接入标准化(AI的USB接口)


Model Context Protocol（MCP，模型上下文协议）是Anthropic的一个开源协议，它标准化了大语言模型与外部工具和数据源通信的方式，允许开发者和工具提供商只需集成一次，就能与任何兼容 MCP 的系统交互。MCP 就像 USB-C 标准：不需要为每个设备使用不同的连接器，而是使用一个端口来处理多种类型的连接。

它解决的核心痛点是：**M 个模型 × N 个工具，原来要 M×N 次定制开发；MCP 只需 M+N 次**——工具实现一次 MCP Server，所有支持 MCP 的模型都能调。

#### MCP vs Function Calling的区别

- Function Calling： 是模型的能力(模型输出结构化的调用意图)
- MCP: 是工具的分发标准(工具怎么被发现、描述、调用)

两者是上下游关系，不是竞争关系: 智能体用Function Calling决定我要调工具，通过MCP找到并执行工具。  


## MCP 架构
MCP 遵循客户端 - 主机 - 服务器架构，架构中包括：
- **MCP Host（主机）**：	协调和管理一个或多个 MCP 客户端的 AI 应用(如 Claude、Cursor、VS Code)
- **MCP Client（客户端）**：	一个保持与 MCP 服务器连接的组件，通过 MCP 定义的消息处理通信，从服务器查找并请求资源和工具，并管理与服务器的连接生命周期
- **MCP Server（服务器）**：一个向 MCP 客户端提供服务的程序，封装并暴露三类核心能力： 
	- **Resources**（资源）：只读数据（文件、数据库、API 返回值）
	- **Tools**（工具）：可执行函数（发邮件、查数据库、调用第三方服务）
	- **Prompts**（提示模板）：预定义工作流


MCP Client 与 MCP Server之间有两种通信协议：

- stdio
- streamable_http

stdio就是标准输入输出，MCP Client运行时，分两种情况：

- 外部服务：Client会把这个MCP服务的脚本下载到本地，然后作为一个子进程运行。    
- 本地服务：Client会把本地脚本直接加载，作为一个子进程运行

也就是说，stdio模式中，MCP Client 和 MCP Server之间的通信就是进程通信，没有网络延迟。

streamable_http其实就是可以用event stream来发送数据的http模式，本质还是Server Event Stream，也就是SSE。也就是说MCP client通过发送http请求与MCP server交互。因此存在一定的网络延迟。



### 精妙的"握手"机制

当 Client 连接到 Server 时，会做一次**发现握手**：

```
Client → tools/list → "你有哪些工具？"
Server → 返回工具清单（名称 + 描述 + 参数 Schema）
```

模型拿到这份清单，就知道当前能调什么、参数怎么传。这解决了 Function Calling 时代"模型不知道有哪些工具"的问题。



## MCP 分为两个层级：

- 数据层
  数据层实现了一个基于 JSON-RPC 2.0 的交换协议，该协议定义了消息结构和语义。
  数据层包括生命周期管理（连接初始化、能力协商、连接终止）、服务器功能（提供工具、资源和提示模板）、客户端功能（调用LLM、获取输入、记录消息）、其他功能（实时更新通知、长时运行操作跟踪）。
- 传输层
  传输层定义了客户端与服务器之间数据交换的通信机制和通道，包括特定传输方式的连接建立、消息帧定界和授权。
  MCP 支持多种传输机制，包括 Stdio、Streamable HTTP、SSE。

## MCP 工作流程

#### 初始化

  在初始化过程中，AI 应用程序的 MCP 客户端管理器连接到配置的服务器，并将它们的能力存储起来以供后续使用。应用程序使用这些信息来确定哪些服务器可以提供特定类型的功能（工具、资源、提示），以及它们是否支持实时更新。
	
初始化有几个重要的作用：

- 协议版本协商	确保客户端和服务器使用兼容的协议版本，避免因版本不一致导致的通信问题
- 能力发现	声明各自支持的功能，包括他们能够处理的基元类型（工具、资源、提示）以及是否支持通知等特性
- 身份交换	交换客户端与服务器的身份及版本信息，便于后续的调试与兼容性管理


#### 工具发现

AI 应用程序从所有连接的 MCP 服务器中获取可用工具，并将它们组合成一个语言模型可以访问的统一工具注册表。这使得 LLM 能够理解它可以执行哪些操作，并在对话期间自动生成相应的工具调用。

连接建立之后，客户端可以通过发送 tools/list 请求来发现可用的工具。这个请求是 MCP 工具发现机制的基础—它允许客户端在尝试使用工具之前了解服务器上有哪些可用的工具。响应包含一个 tools 数组，该数组提供了关于每个可用工具的全面元数据。这种基于数组的结构允许服务器同时展示多个工具，同时保持不同功能之间的清晰界限。响应中的每个工具包括几个关键字段：
- name	工具标识符
- title	工具的易读显示名称
- description	工具描述
- inputSchema	一个定义预期输入参数的 JSON Schema，支持类型验证并提供关于必需和可选参数的清晰文档

#### 工具执行

当语言模型在对话中决定使用工具时，AI 应用程序会拦截工具调用，将其路由到合适的 MCP 服务器，执行该工具，并将结果作为对话流程的一部分返回给 LLM。这使 LLM 能够访问实时数据并在外部世界中执行操作。

客户端使用 tools/call 方法执行一个工具。tools/call 请求遵循结构化格式，确保客户端和服务器之间的类型安全和清晰通信。请求结构包括几个重要组件：
- name	工具标识符
- arguments	包含工具的 inputSchema 定义的输入参数
响应返回一个内容对象数组，允许进行丰富、多格式的响应（文本、图片、资源等）。每个内容对象都有一个 type 字段。

#### 实时更新

MCP 支持实时通知，使服务器能够在未经明确请求的情况下通知客户端有关变更。当 AI 应用程序收到关于工具变更的通知时，它会立即刷新其工具注册表并更新 LLM 的可用功能。这确保了正在进行的对话始终能够访问最新的一组工具，并且 LLM 可以随着新功能的可用而动态适应。




MCP 解决的是"模型↔工具"的连接，但面对**跨厂商、跨框架的多智能体协作**时，它无能为力。比如你的客服 Agent 需要调用另一个公司独立部署的财务 Agent——双方代码不通、框架不同，MCP 管不到这一层。

这就引出了 A2A。


---

# Skill: 把"怎么把事干对"封装成可复用能力

Agent Skill是大模型的说明文档

### 解决了什么问题

MCP 解决了"能调什么工具"，但没解决"怎么把事干对"。

举个例子：用户说"帮我查下有没有低估值 ETF"。模型需要调用基金查询工具，但这个工具要求：

- `peFenWei` 字段要传小数（20% 要传 0.2）
- `fundType` 要严格用枚举值
- 某些字段不能为空

如果每个 Agent 都要重新学一遍这些业务规则，效率极低。Skill 就是来解决这个问题的。

### 本质

一个 Skill 通常是一个**目录**，包含：

- **SKILL.md**：操作手册（什么时候用、有哪些字段、什么不能做）
- **scripts/**：可执行脚本（真正的执行逻辑）

### 精妙的语言分层设计

Skill 最核心的价值是做了**三种语言的翻译**：

|语言|例子|使用者|
|---|---|---|
|用户说的日常人话|"帮我查下有没有低估值 ETF"|用户|
|Skill 写的专业人话|"peFenWei 传小数，20% 传 0.2；fundType 严格用枚举值"|模型|
|模型输出的机器话|`{"field":"peFenWei","type":4,"value":"0.3"}`|工具|

大模型是这三种语言之间的**翻译官兼规则执行者**：听懂用户 → 对齐专家规则 → 输出机器指令。


### 价值

- **能力复用**：一个 Skill 写好，任何 Agent 都能直接获得这项专业能力，不需要理解底层 API 细节
- **减少 LLM 轮次**：把多步操作流程预先封装好，模型不用一步步试错
- **组织知识沉淀**：把"公司这事该怎么办"固化成 Skill，新人 Agent 接入即用

### 与 MCP 的配合

MCP 是"工具的自描述"（告诉模型我有什么工具），Skill 是"业务的操作手册"（告诉模型这事该怎么干）。两者互补：Skill 底层通常会调用多个 MCP 工具来完成一个完整业务流程。



### MCP vs Skill 

MCP给大模型提供数据，而Skill教大模型如何处理数据   

MCP本质是是一个运行程序，而Skill本质是一个说明文档

- MCP connects Claude to data
- Skills teach Claude what to do with that data



---

# A2A： 让智能体之间组队协作

2025 年 4 月，Google 发布了 **A2A（Agent2Agent）协议**，旨在打破智能体"孤岛"。

现实中的复杂任务往往需要多个专家协作。比如要完成一个差旅规划，需要气象 Agent、出行 Agent、铁路 Agent、航空 Agent 等多种应用共同配合。但各智能体的接入和信息传输方式均有区别——**A2A 志在打造智能体时代的 TCP 协议**。

### 核心定位

A2A 的设计哲学和 MCP 截然不同：

- **MCP**：你的智能体**精细控制**每个工具调用（"调这个函数，参数是 X，把结果给我"）
- **A2A**：你把任务**整体委派**给另一个智能体，对方用自己的推理、自己的工具、自己的逻辑独立完成，最后给你一个结构化结果

用 Google 官方的话说：**MCP 给智能体提供工具与上下文，A2A 让智能体互相通信协作，两者互补**。

### 核心概念

A2A 基于 HTTP、SSE、JSON-RPC 等成熟标准构建，包含几个关键组件：

**1. Agent Card（智能体名片）**
每个 A2A 智能体挂出一份标准 JSON 名片——我叫什么、我会什么技能、在哪个地址找我;
JSON 格式的自描述文档，一般托管在 `/.well-known/agent.json`。描述了智能体的名称、功能、URL、版本、输入输出格式、认证要求等。其他 Agent 通过它来"发现"彼此——相当于智能体的"简历"。

**2. Task（任务）**

调用方发任务 → 接收方异步执行 → 中途可以汇报进度、可以追问澄清 → 最终交付。对话式、长任务友好,这是它和"调工具"最大的不同:工具调用是"一问一答",智能体协作是"来回商量"。

A2A 中工作的基本单元。客户端 Agent 创建 Task 委派给远程 Agent，Task 有完整的状态机：`submitted`（已提交）→ `working`（工作中）→ `input-required`（需要输入）→ `completed`（完成）/ `failed`（失败）/ `canceled`（取消）。

关键特性：**支持长周期异步任务**。一个 Task 的生命周期可以长达几天——比如"深度研究某个行业"这种需要数小时甚至数天的任务。

**3. Message 和 Artifact**

Message 是智能体间的对话载体；Artifact 是任务的最终交付物。内容可以是文本、文件、JSON 或表格数据，支持多模态。





### ANP

ANP(Agent Network Protocol):更宏大的愿景——让全网智能体像网站一样可以被发现(类似 DNS/搜索引擎)、互相认证(去中心化身份 DID)、自由组网。目前处于早期,了解理念即可:如果说 MCP/A2A 在修"点对点的路",ANP 想修"整个路网"。

三句话记住分工:

- MCP 管"用工具",
- A2A 管"雇同行",
- ANP 管"找到彼此"

真实系统三者叠着用:

你的智能体通过 ANP 发现一个擅长财报分析的智能体 → 通过 A2A 委托它任务 → 它内部通过 MCP 调用数据库工具完成分析。


