![[Pasted image 20260906160504.png]]


后面所有的内容都以这个图为主线说明： 
- 先是AI的由来
- 然后是为什么要有机器学习，它的由来以及它存在的问题
- 神经网络的由来以及问题
- 深度学习的由来以及问题
- 生成式AI
## 第一层： AI -- 终极愿景

AI，全称是Artificial Intelligence，人工智能，是计算机科学的一个分支，目标是让机器能够像人一样“智能”的思考和行动。  



人工智能(AI, Artificial Inteligence)通常用于描述致力于执行接近人类智能任务(如语音识别、语音翻译和视觉感知)的计算机系统。

它是指软件产生的智能，而不是人类的智能。   

目标是研究、设计和构建具备智能、学习、推理和行动能力的计算机和机器。    

例如: 2016年Alphago击败李世石围棋。     

> AI并不是一个单一技术，而是一个学科领域，里面包含机器学习、自然语言处理、计算机视觉、语音识别、知识识别、机器人学、多智能体等众多子方向。


###  1.1 命名与诞生

1956年夏天，麦卡锡、明斯基、香农等一群科学家在美国达特茅斯学院召开了一场研讨会，正式提出“Artificial Intelligence”这个概念，并确定了AI的核心目标：让机器模拟人类的学习、语言、推理等能力。

这次会议被公认为人工智能的诞生标志。


### 1. 2 GAI: 生成式人工智能

GAI (Generative Aritificial Interligence)

目标是让机器能够产生复杂有结构的物件。 例如chatGPT、deepseek。



---

- **AI的由来**：1956年达特茅斯会议正式提出概念，旨在让机器具备类似人类的智能。
- **解决什么问题**：顶层愿景，试图解决机器感知、推理、规划、交互等广泛的智能任务。外层涵盖了规划调度、专家系统、机器人学、模糊逻辑、AI伦理等广泛领域。
- **存在什么问题**：早期依赖人工编写规则（符号主义），面对复杂多变现实世界时，规则爆炸且泛化极差。
- **为什么继续发展**：手工规则的天花板太低，必须寻找能让机器自动从数据中学习规律的新路径，从而走向了机器学习。


机器学习是AI的一个子领域，也是目前AI最核心的实现方式。  
## 第二层：机器学习 -- 数据驱动



ML(Machine Learning)： 简单说就是从数据中获取规律，来了一个新的数据，产生一个新的预测。 

提到机器学习，追根溯源，我们需要先知道什么是“学习”。著名学者、1975年图灵奖获得者、1978年诺贝尔经济学奖获得者，赫伯特·西蒙（Herbert Simon）教授曾对“学习”下过一个定义:     

如果一个系统，能够通过执行某个过程，就此改进它的性能，那么这个过程就是学习。    

在西蒙看来，学习的核心目的就是改善性能。      

其实对于人而言，这个定义也是适用的。如果我们仅仅进行低层次的重复性学习，而没有达到认知升级的目的，那么即使表面看起来非常勤奋，其实也仅仅是一个“伪学习者”，因为我们的性能并没有得到改善。       
西蒙认为，对于计算机系统而言，通过运用数据及某种特定的方法（比如统计方法或推理方法）来提升机器系统的性能，就是机器学习（Machine Learning，简称ML）。        

所谓机器学习，在形式上可近似等同于，在数据对象中通过统计或推理的方法，寻找一个有关特定输入和预期输出的功能函数f()。   



### 2.1 为什么需要机器学习


传统编程方式是：人总结规则，写成程序，电脑执行。但很多问题根本没法用规则描述。

比如：“这张图片里是不是猫？”你无法写出一套穷尽所有情况的规则，因为猫的姿势、颜色、背景千变万化。但人类可以看很多张猫的图片后自己学会识别。

机器学习的核心思想就是：**不要人写规则，而是让机器从数据中自己学出规则。**

形式化定义：如果一个计算机程序在任务 $T$ 上的性能 $P$，随着经验 $E$ 的增加而提高，那么这个程序就可以被称为在“学习”。这个定义来自卡内基梅隆大学教授Tom Mitchell。

### 2.2 机器学习的三大要素

任何一个机器学习系统都包含：

- **数据**：带有输入输出的样本。数据决定了模型能学到什么。
- **模型**：一个带参数的可计算函数，用来描述输入到输出的关系。
- **学习算法**：通过数据不断调整模型参数，使模型的预测越来越准确。

训练的本质就是最优化：定义一个衡量预测好坏的损失函数，然后利用梯度下降等算法逐步调整参数，让损失尽量小。

### 2.3 机器学习的主要类型

**监督学习**​

训练数据包含输入和正确答案（标签）。模型学习输入到输出的映射。

- 分类：垃圾邮件识别（邮件→垃圾/非垃圾）
- 回归：房价预测（特征→价格）

**无监督学习**​

训练数据没有标签，模型自己发现数据中的结构。

- 聚类：把用户分成不同群体
- 降维：压缩高维数据
- 异常检测：找出离群数据

**半监督学习**​

少量标注数据 + 大量未标注数据，适合标注成本高的情况。

**强化学习**​

智能体与环境不断交互，通过试错获得奖励信号，学习最优策略。

- 游戏AI、机器人控制、自动驾驶决策


所有的监督学习（Supervised Learning），基本上都是分类（Classification）的代名词。

它从有标签的训练数据中学习模型，然后对某个给定的新数据利用模型预测它的标签。        

比如，小时候父母告诉我们某个动物是猫、狗或猪，然后在我们的大脑里就会形成或猫或狗或猪的印象（相当于模型构建），当面前来了一只“新”小狗时，如果你能叫出“这是一只小狗”，那么恭喜你，标签分类成功！         
但如果你回答说“这是一头小猪”，这时你的父母就会纠正你的偏差：“乖，不对，这是一只小狗。”这样一来二去地进行训练，不断更新你大脑的认知体系，当下次再遇到这类新的猫、狗、猪时，你就会在很大概率上做出正确的“预测”分类。    
监督学习，就是先利用有标签的训练数据学习得到一个模型，然后使用这个模型对新样本进行预测。在本质上，监督学习的目标在于，构建一个由输入到输出的映射，该映射用模型来表示。      

#### 监督学习

- 样本(sample)： 一行数据就是一个样本，多个样本组成数据集，有时一条样本也被叫做一条记录
- 特征(feature)： 一列数据一个特征，有时也被称为属性，例如房子的位置、房子的面积
- 标签(lable)： 模型要预测的那一列数据，例如房价


#### 机器学习建模流程

1. 获取数据
2. 数据基本处理
3. 特征工程(特征提取、特征预处理、特征降维、特征选择、特征组合)
4. 机器学习(模型训练)
5. 模型评估

#### 拟合

用在机器学习领域，用来表示模型对样本点的拟合情况。 

- 拟合(fitting)
- 欠拟合(under-fitting): 模型在训练集上表现很差，在测试集表现也很差
- 过拟合(over-fitting)：模型在训练集上表现很好，在测试集表现很差


#### 机器学习

- **解决什么问题**：放弃硬编码规则，让计算机从数据中自动学习规律并做出预测。这一层包含了决策树、支持向量机、集成学习、特征工程，以及无监督、半监督、强化学习等范式。
- **存在什么问题**：严重依赖人工特征工程。面对图像像素、语音波形等高维复杂原始数据时，人类无法设计出完美的特征提取算子。
- **为什么继续发展**：为了摆脱对人类专家设计特征的依赖，需要模型能够自动从数据中抽取特征，于是引入了神经网络。

## 第三层： 神经网络 -- 自动特征提取

- **解决什么问题**：模仿生物神经元连接，通过多层权重叠加和非线性激活，实现对复杂函数的拟合。这一层包含了感知机、多层感知机、反向传播、激活函数、CNN、LSTM/RNN等。
- **存在什么问题**：早期训练极其困难。单层感知机无法解决非线性问题；多层网络缺乏有效训练算法，且随着层数加深会出现严重的梯度消失或爆炸；RNN难以建模长距离依赖且无法并行计算。
- **为什么继续发展**：为了训练真正强大、深层的网络，必须解决深层网络的退化和梯度问题，由此进入了深度学习时代。

## 第四层：深度学习 -- 深层特征

> 不依赖人工设计特征，而是让模型从原始数据中逐层自动抽象出特征。   


传统机器学习算法依赖人工设计特征，并进行特征提取。 

而深度学习不需要人工，而是依赖算法自动提取特征。   

深度学习通过模仿人脑的神经网络来处理和分析复杂的数据，从大量数据中自动提取复杂特征，这也是深度学习被看做黑盒子，可解释性查的原因。 
深度学习尤其擅长处理高维数据，如图像、语音和文本。   


DL(Deep Learning): 深度学习算法试图模拟人类大脑的工作方式，其灵感来源于神经生物学，它通过对大量数据的学习，自动提取出数据的高层次特征和模式，从而实现图像识别、语音识别、自然语言处理等任务。 

比如识别人脸：

- 第一层学到像素级边缘。
- 第二层组合边缘形成眼睛、鼻子等部件。
- 第三层组合部件形成完整面部结构。
- 最后一层输出“这是谁”。

这种从低层到高层、从具体到抽象的学习方式，就是“深度”的含义。


###  深度学习和机器学习、神经网络的关系

可以用一个层级结构理解：

- **AI**：最广泛的概念，追求机器智能。
- **机器学习**：用数据驱动方式实现AI的技术路径。
- **深度学习**：用多层神经网络实现机器学习的一种方式。
- **神经网络**：深度学习的基本模型框架，比如FNN、CNN、RNN、Transformer。

也就是说：**深度学习是机器学习的一种，而神经网络是深度学习的载体。但不是所有神经网络都算深度学习——只有足够深、足够强大的神经网络才算。**


按照架构的不同，神经网络可以分为:   

- 卷积神经网络(CNNS)
- 循环神经网络(RNNS)
- Transformer网络等

同样是区分不同水果，这次你带孩子去了超市，那里有各种不同的水果，你没有解释每种水果的特点，只是给孩子指出了哪些是苹果、哪些是香蕉，他通过观察和比较，慢慢学会了辨认各种水果。 

在这个过程中，孩子的大脑(在这里比喻为深度学习模型)自动从复杂的视觉、嗅觉等信号中提取层次化的特征。比如圆形、条纹、颜色深浅、气味等，从而达到识别水果的目的。  

深度学习特点:   

- 多层非线性变换: 深度学习模型由多个层次组成，每一层都引用非线性激活函数对输入数据进行变换。     
较低的层级通常捕捉到简单的特征(如边缘、颜色等)，而更高的层级则可以识别更复杂的模式(如物体或面部识别)

- 自动特征提取:  与传统机器学习算法不同，深度学习能够自动从原始数据中学习到有用的特征，而不需要人工特征工程，这使得深度学习在许多领域中表现出色。 

- 大数据和计算能力: 深度学习模型通常需要大量的标注数据和强大的计算资源(如GPU)来进行训练。大数据和高性能计算使得深度学习在图像识别、自然语言处理等领域取得了显著突破。 

- 可解释性查: 深度学习模型内部的运作机制相对不透明，被称为“黑箱”，这意味着理解模型为什么做出特定决策可能会比较困难。这对某些应用场景来说是一个挑战。


#### 深度学习
- **解决什么问题**：使用足够深的多层网络自动进行分层特征抽象（从边缘到部件到整体）。这一层包含了深度神经网络、深度卷积网络、生成对抗网络、迁移学习、Dropout等关键技术。
- **存在什么问题**：虽然CNN和RNN解决了部分局限，但CNN感受野有限，RNN串行计算极慢。面对海量互联网数据和超长文本序列，传统深度学习模型在全局长距离依赖建模和训练效率上达到瓶颈。
- **为什么继续发展**：需要一种能并行计算、且能直接建模任意长距离依赖的架构，Transformer应运而生，并推动了生成式AI的爆发。



##### 深度学习的常见模型

| 模型                 | 核心特点         | 主要应用       |
| ------------------ | ------------ | ---------- |
| FNN（多层感知机）         | 全连接、非线性映射    | 表格数据、特征分类  |
| CNN（卷积神经网络）        | 局部感知、权值共享    | 图像识别、视频理解  |
| RNN/LSTM/GRU（循环网络） | 时间记忆、序列建模    | 语音、文本、时间序列 |
| Transformer        | 自注意力、全局建模、并行 | 自然语言、大模型   |
| GAN（生成对抗网络）        | 生成器与判别器对抗    | 图像生成、数据增强  |
| VAE（变分自编码器）        | 概率编码解码       | 图像生成、特征学习  |
| Diffusion（扩散模型）    | 逐步去噪生成       | 文生图、视频生成   |



## 第五层：生成式AI（Generative AI）—— 内容创造

- **解决什么问题**：不再局限于判别和分类，而是学习数据分布来创造全新的内容。核心基石包含了Transformer架构、自注意力机制、语言建模、文本生成、文本摘要、对话系统等。
- **现状与未来**：基于Transformer的大语言模型展现了强大的涌现能力。但当前仍面临幻觉、推理可靠性、高算力消耗等挑战。未来的发展正朝着多模态、具身智能以及更高效率的通用人工智能迈进。




---


### 人工智能、机器学习和深度学习

- 人工智能（AI）是计算机科学的一个广泛领域，目标是让机器具备类人的“智能”，包括自然语言处理（NLP）、计算机视觉、机器人技术、专家系统等等；具体实现手段多种多样，包括规则系统、符号逻辑、统计方法、机器学习等。
- 机器学习是AI的一个子领域，核心是通过数据驱动，让计算机进行学习并改进性能，自动发现规律（模式），并利用这些规律进行预测或决策。
- 深度学习是机器学习的一个子领域，基于多层神经网络处理复杂任务。





































